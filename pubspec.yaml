name: mini_getx
description: The fork getx, this is a mini version getx, Only retain state management, remove route、connect、bing...
version: 5.0.2
homepage: https://github.com/jonataslaw/getx
repository: https://github.com/luoyi58624/getx.git

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  web: any

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
